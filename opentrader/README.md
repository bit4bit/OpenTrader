# opentrader/

The Django project package — configuration and entry points.

## Overview

Wires everything together: project settings, authentication, root URL routing (API requests to the `market` app, everything else to the frontend SPA), and the WSGI/ASGI deployment entry points.
